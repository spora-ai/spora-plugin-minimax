includes:
    - vendor/phpstan/phpstan-mockery/extension.neon
    - phpstan-baseline.neon

parameters:
    paths:
        - src
        - tests
    level: 5
    excludePaths:
        - vendor
    bootstrapFiles:
        - vendor/autoload.php
